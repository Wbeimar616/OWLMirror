rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For development, allow read/write to all paths.
    // In production, you should lock this down.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
